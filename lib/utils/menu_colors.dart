import 'package:flutter/material.dart';

class MenuColors {
  // Glass effect colors for light theme
  static final Color glassBackgroundLight = Colors.white.withOpacity(0.6);
  static final Color glassLight = Colors.white.withOpacity(0.4);
  
  // Glass effect colors for dark theme
  static final Color glassBackgroundDark = Colors.black.withOpacity(0.6);
  static final Color glassDark = Colors.black.withOpacity(0.4);
} 